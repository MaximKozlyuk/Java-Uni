package lab5.barBossHouse.tests;

import lab5.barBossHouse.InternetOrder;
import org.junit.Assert;
import org.junit.Before;
import org.junit.jupiter.api.Test;

public class InternetOrderTest {

    private Tests tests = new Tests();
    private InternetOrder internetOrder = tests.internetOrder;

    @Test
    void testIndexOf () {
       // if (internetOrder.indexOf(tests.items[1]) != 1) { Assert.fail(); }
    }

}
